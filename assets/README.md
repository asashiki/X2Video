# Optional assets

Drop a looping bed track here as `bgm.mp3` if you want unattended renders to mix quiet background music under the TTS.

- Keep it instrumental.
- Volume is ducked automatically (~10%).
- If the file is missing, render continues without BGM.

You can also set `bgm_path` in `x2video.toml`.
