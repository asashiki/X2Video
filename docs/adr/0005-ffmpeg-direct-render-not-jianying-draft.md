# 合成用 FFmpeg 直出成品 MP4，而非剪映草稿

视频合成直接用 FFmpeg 渲染出可发布的 1080×1920 MP4，不生成剪映草稿。卡片解说视频是模板化画面（背景 + 卡片 + 字幕 + 配音），模板调好后逐条微调的需求极小，剪映草稿的人工可控性优势用不上，却引入重依赖和已知的深坑（三 ID 一致性、绝对路径、版本兼容），且与全自动定时任务模式冲突。若实测发现确有手工修片需求，再补"导出剪映草稿"旁路（后期增强）。
